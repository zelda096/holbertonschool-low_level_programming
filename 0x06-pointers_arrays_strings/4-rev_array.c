#include "holberton.h"
/**
 * reverse_array - function for rev
 *@a: pointer variable
 *@n: parameter
 *Return: 0
 */
void reverse_array(int *a, int n)
{
	
}
{
	int x = 0;
	int y = n - 1;
	int tmp;

	while (i < j)
	{
		tmp = a[x];
		a[x] = a[y];
		a[y] = tmp;
		x++;
		y--;
	}
}
